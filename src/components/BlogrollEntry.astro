---
import type { CollectionEntry } from "astro:content";
import { shortenLink } from "./utils";

interface Props {
    blog: CollectionEntry<"blogroll">;
}
const { blog } = Astro.props;
const { Content } = await blog.render();
---

<fieldset id={blog.slug}>
    <legend>
        <strong>
            {blog.data.pin ? "📌 " : ""}
            {blog.data.title}</strong
        >
        <a href={blog.data.link} target="_blank"
            >{shortenLink(blog.data.link)}</a
        >
    </legend>
    <Content />
</fieldset>
